services:
  carflip:
    build: .
    # Todas las variables (DATABASE_URL hacia Supabase, R2, MercadoLibre, etc.)
    # se leen directamente desde tu .env local — no se duplican aquí.
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    command: ["run"]
