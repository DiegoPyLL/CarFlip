services:
  carflip:
    build: .
    # Todas las variables en .env
    # se leen directamente desde tu .env local — no se duplican aquí.
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    command: ["run"]
