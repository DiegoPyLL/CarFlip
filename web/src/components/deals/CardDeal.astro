---
import type { CategoriaDeal, Deal } from '@lib/tipos';
import { resolverUrlImagen } from '@lib/cdn';
import { formatearPrecio, formatearKm } from '@lib/formato';

interface Props {
  deal: Deal;
}

const { deal } = Astro.props;
const urlImagen = resolverUrlImagen(deal.url_imagen);

const fuenteLabels: Record<Deal['fuente'], string> = {
  autocosmos: 'Autocosmos',
  yapo: 'Yapo',
  mercadolibre: 'MercadoLibre',
};

const categoriaEstilos: Record<CategoriaDeal | 'sin_evaluar', { label: string; clases: string }> = {
  oportunidad_clara: { label: 'Oportunidad', clases: 'bg-green-500 text-zinc-950' },
  buen_precio:       { label: 'Buen precio', clases: 'bg-amber-400 text-zinc-900' },
  revisar:           { label: 'Revisar',     clases: 'bg-zinc-700 text-zinc-200' },
  descartar:         { label: 'Descartar',   clases: 'bg-red-500 text-zinc-950' },
  sin_evaluar:       { label: 'Sin evaluar', clases: 'bg-zinc-800 text-zinc-400' },
};

const badge = categoriaEstilos[deal.categoria ?? 'sin_evaluar'];

const pctMercado = deal.pct_vs_mercado;
const riesgosVisibles = deal.riesgos.slice(0, 3);
const riesgosExtra = deal.riesgos.length - riesgosVisibles.length;

const metaItems = [
  deal.anio ? String(deal.anio) : null,
  deal.km !== null ? formatearKm(deal.km) : null,
  deal.ubicacion ?? null,
].filter(Boolean);
---

<article class="group bg-zinc-900 rounded-2xl overflow-hidden border border-zinc-800/50 hover:border-zinc-700 transition-all duration-200 hover:-translate-y-0.5 hover:shadow-xl hover:shadow-zinc-950/60">
  <a
    href={deal.url}
    target="_blank"
    rel="noopener noreferrer"
    aria-label={`Ver ${deal.titulo} en ${fuenteLabels[deal.fuente]}`}
    class="block"
  >
    <!-- Imagen con badge de categoría IA -->
    <div class="aspect-[16/10] bg-zinc-800 relative overflow-hidden">
      {urlImagen ? (
        <img
          src={urlImagen}
          alt={deal.titulo}
          width={640}
          height={400}
          loading="lazy"
          class="w-full h-full object-cover group-hover:scale-[1.03] transition-transform duration-300 ease-out"
        />
      ) : (
        <div class="w-full h-full flex items-center justify-center text-zinc-700 text-sm">Sin imagen</div>
      )}

      <!-- Badge de categoría: protagonista en esta pantalla -->
      <div class="absolute top-3 right-3">
        <span class={`text-sm font-black px-3 py-1.5 rounded-lg shadow-lg ${badge.clases}`}>
          {badge.label}
        </span>
      </div>

      <!-- Fuente -->
      <div class="absolute top-3 left-3">
        <span class="text-xs font-semibold px-2.5 py-1 rounded bg-zinc-950/75 text-zinc-300 backdrop-blur-sm">
          {fuenteLabels[deal.fuente]}
        </span>
      </div>
    </div>

    <!-- Contenido -->
    <div class="p-5">
      <h2 class="text-base font-medium text-zinc-200 line-clamp-2 leading-snug mb-4">
        {deal.titulo}
      </h2>

      <!-- Precio + puntaje IA -->
      <div class="flex items-baseline justify-between gap-2 mb-1.5">
        <span class="text-2xl font-bold text-amber-400 tabular-nums">
          {formatearPrecio(deal.precio, deal.moneda)}
        </span>
        {deal.puntaje !== null && (
          <span class="text-base font-black text-zinc-300 tabular-nums" title="Puntaje IA (0-100)">
            {deal.puntaje}<span class="text-xs font-semibold text-zinc-600">/100</span>
          </span>
        )}
      </div>

      <!-- Precio vs mercado -->
      {pctMercado !== null && pctMercado < 0 && (
        <p class="text-sm font-semibold text-green-500 mb-1 tabular-nums">
          ▼ {Math.abs(pctMercado).toFixed(1)}% vs mercado
          {deal.precio_mercado !== null && (
            <span class="text-zinc-600 font-normal"> ({formatearPrecio(deal.precio_mercado, deal.moneda)})</span>
          )}
        </p>
      )}

      <!-- Bajada de precio propia del aviso -->
      {deal.delta_pct !== null && deal.delta_pct < 0 && (
        <p class="text-xs text-green-600 mb-1 tabular-nums">
          Bajó {Math.abs(deal.delta_pct).toFixed(1)}% desde que lo vimos
        </p>
      )}

      <!-- Riesgos detectados por la IA -->
      {riesgosVisibles.length > 0 && (
        <div class="flex flex-wrap gap-1.5 mt-3 mb-1">
          {riesgosVisibles.map((riesgo) => (
            <span class="text-xs font-medium px-2 py-1 rounded bg-red-500/10 text-red-400 border border-red-500/20">
              {riesgo}
            </span>
          ))}
          {riesgosExtra > 0 && (
            <span class="text-xs font-medium px-2 py-1 rounded bg-zinc-800 text-zinc-500">
              +{riesgosExtra}
            </span>
          )}
        </div>
      )}

      <!-- Resumen IA -->
      {deal.resumen && (
        <p class="text-sm text-zinc-500 line-clamp-2 mt-3 mb-1">{deal.resumen}</p>
      )}

      {metaItems.length > 0 && (
        <p class="text-sm text-zinc-600 truncate mt-3">{metaItems.join(' · ')}</p>
      )}
    </div>
  </a>
</article>
